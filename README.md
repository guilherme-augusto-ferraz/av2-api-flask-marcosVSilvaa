# 🚀 API de Lista de Tarefas Diárias (Task List API)

Esta é uma API RESTful simples desenvolvida em Python com o framework Flask e a extensão Flask-SQLAlchemy para persistência de dados. O objetivo é fornecer endpoints para gerenciamento completo de tarefas (CRUD), incluindo autenticação JWT para garantir que cada usuário gerencie apenas suas próprias tarefas.

---

## 🛠️ Instruções de Instalação e Execução

Para configurar e executar o projeto localmente, siga os passos abaixo:

### 1. Pré-requisitos

Certifique-se de ter o **Python 3.8+** e o **Git** instalados.

### 2. Configuração do Ambiente

Crie e ative um ambiente virtual (recomendado):

```bash
# Cria o ambiente virtual
python -m venv venv

# Ativa o ambiente virtual (Linux/macOS)
source venv/bin/activate 

# Ativa o ambiente virtual (Windows)
.\venv\Scripts\activate